
The header file syscall_header.h has the declaration of the struct that is common between the system-call (my_syscall.c) and the user-space program (main.c).

This system call displays 200 processes in ps -e format.

